
  # RealMaker Web (Vite)

  Web client built from the `/reference` prototype. Uses Vite + React and the same UI components/styles from the reference bundle.

  ## Running the code

  ```bash
  cd client-web
  npm install
  npm run dev
  ```
  
